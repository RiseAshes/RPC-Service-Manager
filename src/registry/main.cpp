#include "registry_server.h"
#include <iostream>

int main(int argc, char* argv[]) {
    uint16_t port = 8080;  // 默认端口

    // TODO: 解析命令行参数

    std::cout << "RPC 服务注册中心启动中..." << std::endl;
    std::cout << "端口: " << port << std::endl;

    // TODO: 初始化并启动注册中心服务器

    return 0;
}